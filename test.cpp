#include <bits/stdc++.h>
#include <unistd.h>

using namespace std;



int main() {
    FILE* file = fopen("asyik.txt", "r");
	if (file == NULL) {
		exit(0);
	}

    bool udahan = false;
    unsigned char c;

    string s;
    s.c
    while (fscanf(file, "%c", &c) != EOF) {
    }
}